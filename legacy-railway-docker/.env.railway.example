# Database: set these as reference variables from Railway's MySQL service.
MYSQLHOST=${{MySQL.MYSQLHOST}}
MYSQLPORT=${{MySQL.MYSQLPORT}}
MYSQLUSER=${{MySQL.MYSQLUSER}}
MYSQLPASSWORD=${{MySQL.MYSQLPASSWORD}}
MYSQLDATABASE=${{MySQL.MYSQLDATABASE}}

# Gmail SMTP (use a Google app password; do not commit the real value).
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=tu_correo@gmail.com
MAIL_PASSWORD=PEGA_AQUI_LA_CONTRASENA_DE_APLICACION
MAIL_FROM_NAME=La Pesquera
